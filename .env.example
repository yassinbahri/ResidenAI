APP_ENV=local
DATABASE_URL=postgresql+psycopg://residency:residency@localhost:5433/residency_tracker

# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
# Single-operator auth - the one token for the human (frontend). Never
# commit a real value.
ADMIN_TOKEN=

SCHEDULER_INTERVAL_SECONDS=900
